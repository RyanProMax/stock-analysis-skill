{
  "version": 1,
  "commands": {
    "hkipo": {
      "description": "自动分析当前可认购与待上市港股 IPO 池",
      "entrypoints": ["im", "web"],
      "executor": {
        "command": "python3",
        "args": ["commands/hkipo.py"]
      }
    },
    "cnipo": {
      "description": "预留 A 股 IPO 指令位（暂未实现）",
      "entrypoints": ["im", "web"],
      "executor": {
        "command": "python3",
        "args": ["commands/cnipo.py"]
      }
    },
    "research": {
      "description": "生成单只股票深度研报",
      "entrypoints": ["im", "web"],
      "executor": {
        "command": "python3",
        "args": ["commands/research.py"]
      }
    }
  }
}
